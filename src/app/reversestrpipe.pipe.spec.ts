import { ReversestrpipePipe } from './reversestrpipe.pipe';

describe('ReversestrpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReversestrpipePipe();
    expect(pipe).toBeTruthy();
  });
});
